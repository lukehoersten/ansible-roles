---

bin_version: "2.0.1"
# https://github.com/w4/bin/releases
bin_tar: "https://github.com/w4/bin/archive/refs/tags/v{{bin_version}}.tar.gz"
bin_build_dir: "{{build_work_dir}}/bin-{{bin_version}}"
bin_srv_dir: "{{build_srv_dir}}"
